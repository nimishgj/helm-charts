{{- if .Values.javaSpringboot.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-helm.java-springboot.fullname" . }}
  labels:
    {{- include "hello-helm.java-springboot.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.javaSpringboot.replicas }}
  selector:
    matchLabels:
      {{- include "hello-helm.java-springboot.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.common.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "hello-helm.java-springboot.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.common.serviceAccount.create }}
      serviceAccountName: {{ include "hello-helm.serviceAccountName" . }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.global.securityContext | nindent 8 }}
      containers:
      - name: java-springboot
        securityContext:
          runAsUser: 0
          runAsNonRoot: false
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
        image: {{ include "hello-helm.image" (dict "Values" .Values "name" .Values.javaSpringboot.image.name) }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.javaSpringboot.image.targetPort }}
          protocol: TCP
        livenessProbe:
          httpGet:
            path: {{ .Values.javaSpringboot.healthCheck.path }}
            port: {{ .Values.javaSpringboot.healthCheck.port }}
          initialDelaySeconds: {{ .Values.javaSpringboot.healthCheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.javaSpringboot.healthCheck.periodSeconds }}
          timeoutSeconds: {{ .Values.javaSpringboot.healthCheck.timeoutSeconds }}
        readinessProbe:
          httpGet:
            path: {{ .Values.javaSpringboot.healthCheck.path }}
            port: {{ .Values.javaSpringboot.healthCheck.port }}
          initialDelaySeconds: {{ .Values.javaSpringboot.healthCheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.javaSpringboot.healthCheck.periodSeconds }}
          timeoutSeconds: {{ .Values.javaSpringboot.healthCheck.timeoutSeconds }}
        resources:
          {{- toYaml .Values.javaSpringboot.resources | nindent 12 }}
        env:
        - name: SERVER_PORT
          value: {{ .Values.javaSpringboot.image.targetPort | quote }}
        - name: SPRING_PROFILES_ACTIVE
          value: "production"
      {{- with .Values.common.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.common.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.common.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}