{{- if .Values.pythonFastapi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hello-helm.python-fastapi.fullname" . }}
  labels:
    {{- include "hello-helm.python-fastapi.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.pythonFastapi.replicas }}
  selector:
    matchLabels:
      {{- include "hello-helm.python-fastapi.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.common.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "hello-helm.python-fastapi.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.common.serviceAccount.create }}
      serviceAccountName: {{ include "hello-helm.serviceAccountName" . }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.global.securityContext | nindent 8 }}
      containers:
      - name: python-fastapi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: false
        image: {{ include "hello-helm.image" (dict "Values" .Values "name" .Values.pythonFastapi.image.name) }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.pythonFastapi.image.targetPort }}
          protocol: TCP
        livenessProbe:
          httpGet:
            path: {{ .Values.pythonFastapi.healthCheck.path }}
            port: {{ .Values.pythonFastapi.healthCheck.port }}
          initialDelaySeconds: {{ .Values.pythonFastapi.healthCheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.pythonFastapi.healthCheck.periodSeconds }}
          timeoutSeconds: {{ .Values.pythonFastapi.healthCheck.timeoutSeconds }}
        readinessProbe:
          httpGet:
            path: {{ .Values.pythonFastapi.healthCheck.path }}
            port: {{ .Values.pythonFastapi.healthCheck.port }}
          initialDelaySeconds: {{ .Values.pythonFastapi.healthCheck.initialDelaySeconds }}
          periodSeconds: {{ .Values.pythonFastapi.healthCheck.periodSeconds }}
          timeoutSeconds: {{ .Values.pythonFastapi.healthCheck.timeoutSeconds }}
        resources:
          {{- toYaml .Values.pythonFastapi.resources | nindent 12 }}
        env:
        - name: PORT
          value: {{ .Values.pythonFastapi.image.targetPort | quote }}
        - name: ENVIRONMENT
          value: "production"
      {{- with .Values.common.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.common.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.common.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}